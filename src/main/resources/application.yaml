spring:
  application:
    name: taco-cloud

  datasource:
    url: jdbc:postgresql://localhost:5432/tacocloud
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
